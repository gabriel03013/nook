/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep this file intentionally empty for now.
  // Add config here as the app grows (images, redirects, headers, etc).
  reactStrictMode: true,
};

module.exports = nextConfig;
